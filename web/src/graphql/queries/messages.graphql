query Messages($options: MessagesInput!) {
  messages(options: $options) {
    hasMore
    messages {
      uuid
      text
      createdAt
      sender {
        uuid
        username
      }
      recipient {
        uuid
        username
      }
    }
  }
}
